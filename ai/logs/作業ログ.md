## 2026-05-27

### Phase1 Player 基本実装

- `PlayerStats` を作成し、最大HP、現在HP、最大Shield、現在Shield、移動速度を管理するようにした。
- `Player` を作成し、位置、移動、HP処理、Shield処理を実装した。
- Core層では Console、描画、入力、ファイル操作に依存しない形にした。
- xUnit想定の `PlayerTests` を追加し、初期化、移動、HP補正、Shield補正を確認するテストを用意した。
- `docs/03_タスク.md` の Phase1 Player 対象項目を更新した。

### Phase1 Step1 最小ゲーム起動

- `ProjectEclipsion.App` プロジェクトを作成し、ソリューションへ追加した。
- `Program.cs` で `GameState`、`ConsoleRenderer`、`GameLoop` を組み立てて起動するようにした。
- Core層に `GameState`、`GameLoop`、`Rendering/IRenderer` を実装した。
- App層に `ConsoleRenderer` を実装し、`Project Eclipsion` のタイトルをコンソール表示するようにした。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を使用しない構成にした。
- `GameStateTests` を追加し、タイトル初期値を確認するテストを用意した。
- `docs/05_用語集.md` に `IRenderer` と `GameLoop` を追記した。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj` で `Project Eclipsion` の表示を確認した。

### Phase1 Step1 動作確認用一時停止

- `Program.cs` にタイトル表示後の Enterキー待ちを追加した。
- 表示文言は `Enterキーで終了します...` とした。
- Core層は変更していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj` でタイトル表示後に Enter 待ち文言が表示されることを確認した。

## 2026-05-30

### Phase1 Step2 プレイヤー表示

- 既存の `PlayerStats` と `Player` 実装を確認し、作り直しは行わなかった。
- `GameState` に既定の `Player` を追加し、Player管理をCore層に置いた。
- `ConsoleRenderer` でタイトルに加えて、Player座標、HP、Shieldを表示するようにした。
- `GameStateTests` にPlayer保持と初期HP/Shield確認を追加した。
- `PlayerTests` にPlayer生成テストを追加した。
- `docs/05_用語集.md` に `Player` と `PlayerStats` を追記した。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を使用していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln --no-build` が成功した。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj` でタイトル、Player座標、HP、Shieldの表示を確認した。

## 2026-06-06

### Phase1 Step3 プレイヤー移動確認

- App層に `KeyboardInput` を追加し、WASD入力を移動方向へ変換するようにした。
- `GameState.MovePlayer` を追加し、入力から直接 `Player.Move` を呼ばずに `GameState` 経由でPlayerへ委譲する流れにした。
- `Program.cs` は `KeyboardInput -> GameState -> GameLoop/Renderer` の呼び出し順を組み立てるだけにし、Playerを直接操作しない形にした。
- `ConsoleRenderer` は現在のPlayer座標、HP、Shieldの表示のみを担当する形を維持した。
- `PlayerTests` にMoveUp、MoveDown、MoveLeft、MoveRightのテストを追加した。
- `GameStateTests` にGameState経由移動テストを追加した。
- `docs/05_用語集.md` に `KeyboardInput` を追記した。
- Core層ではConsole、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj` にW入力を渡し、Player座標のYが `0` から `-1` に変わることを確認した。

### Phase1 Step4 ダメージ処理確認

- `Player.TakeDamage` を追加し、Playerがダメージ命令を受け取れるようにした。
- `PlayerStats.TakeDamage` を追加し、Shieldを先に減らし、Shield超過分のみHPへ入るようにした。
- HPは0未満にならないように既存の補正処理を使い、HPが0の場合に `IsDead` が `true` になるようにした。
- `GameState.DamagePlayer` を追加し、App層から直接Playerを操作せずGameState経由でダメージを与える流れにした。
- `KeyboardInput` にTキーのデバッグ用ダメージ入力を追加した。入力クラスはダメージ要求を返すだけで、Playerは直接操作していない。
- `Program.cs` はTキー入力時に `GameState.DamagePlayer(10)` を呼び出すだけにし、ダメージ計算は持たせていない。
- `ConsoleRenderer` に `IsDead` 表示を追加し、HP / Shield / 死亡状態を確認できるようにした。
- `PlayerTests` にShield優先ダメージ、Shield超過分HP反映、HP下限0、死亡判定のテストを追加した。
- `GameStateTests` にGameState経由でPlayerへダメージを与えるテストを追加した。
- Core層ではConsole、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln --no-restore` が成功した。
- `dotnet test ProjectEclipsion.sln --no-build` が成功した。結果は20件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` にT入力を渡し、Shieldが `50` から `40` に減ることを確認した。
- T連続入力でShieldが0になった後にHPが減り、HPが0になると `IsDead: True` が表示されることを確認した。

## 2026-06-25

### Phase1 Step5 武器・弾の最小実装

- Core層に `WeaponStats`、`WeaponCategory`、`BulletType`、`Weapon`、`Bullet` を実装した。
- Phase1仕様として `WeaponCategory.Assault` と `BulletType.Normal` のみを用意した。
- `Weapon.Fire` で発射位置、方向、速度、ダメージを持つ通常弾を生成するようにした。
- `Bullet.Update` で方向と速度に応じて座標を更新するようにした。
- `GameState` に現在武器と弾一覧を追加し、`FireCurrentWeapon` と `Update` で発射と弾移動を管理するようにした。
- `KeyboardInput` は Spaceキーを発射要求として返すだけにし、WeaponやBulletを直接操作しない形にした。
- `Program.cs` は入力結果を `GameState` へ渡すだけにし、弾生成ロジックは持たせていない。
- `ConsoleRenderer` で弾座標を `Bullet: (X, Y)` と表示するようにした。
- `WeaponTests` に発射、初期座標、弾タイプ、弾移動のテストを追加した。
- `GameStateTests` に現在武器、GameState経由発射、GameState.Updateによる弾移動のテストを追加した。
- `docs/05_用語集.md` に `WeaponStats`、`WeaponCategory`、`BulletType`、`Weapon` を追記した。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。結果は27件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` にSpace入力を渡し、`Bullet: (1, 0)`、`Bullet: (2, 0)`、`Bullet: (3, 0)` とX座標が増えることを確認した。

### Phase1 Step6 敵の表示・移動

- Core層に `EnemyAiState`、`EnemyAiLevel`、`Enemy` を実装した。
- Phase1仕様として `EnemyAiLevel.Basic`、`EnemyAiState.Idle`、`EnemyAiState.Combat` のみを用意した。
- `Enemy` で座標、最大HP、現在HP、AIレベル、AI状態を管理するようにした。
- `Enemy.Update` でAI状態を `Combat` に変更し、プレイヤー座標へX/Y方向に1マスずつ接近するようにした。
- `Enemy.TakeDamage` でHPを減らし、HPが0未満にならないようにした。
- `GameState` に初期Enemyを1体追加し、Enemy一覧を管理するようにした。
- `GameState.Update` で既存のBullet更新に加えてEnemy更新を呼び出すようにした。
- `ConsoleRenderer` で `Enemy: (X, Y) HP: N State: StateName` を表示するようにした。
- `EnemyTests` を追加し、初期HP、AIレベル、AI状態遷移、X/Y接近移動、HP処理、死亡判定を確認するテストを用意した。
- `GameStateTests` にGameState経由でEnemyを保持し、Updateで移動するテストを追加した。
- `docs/05_用語集.md` に `Enemy`、`EnemyAiState`、`EnemyAiLevel` を追記した。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。結果は37件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` に入力を渡し、初期表示で `Enemy: (30, 10) HP: 30 State: Idle`、更新後に `Enemy: (29, 9) HP: 30 State: Combat`、`Enemy: (28, 8) HP: 30 State: Combat` と表示されることを確認した。

### Phase1 Step7 当たり判定

- `Bullet` に `IsActive` と `Deactivate` を追加し、命中後に非アクティブ化できるようにした。
- `GameState.Update` でBullet更新、Enemy更新、Bullet/Enemy衝突判定、非アクティブBullet削除、死亡Enemy削除を行うようにした。
- BulletとEnemyの整数座標が一致した場合に `Enemy.TakeDamage(Bullet.Damage)` を呼び、Bulletを非アクティブ化するようにした。
- 非アクティブなBulletは `GameState.Update` 内で削除するようにした。
- HPが0になったEnemyは `GameState.Update` 内で削除するようにした。
- PlayerとEnemyの接触ダメージ、爆発、貫通、レーザー、追尾弾、Score加算は実装していない。
- `WeaponTests` にBullet非アクティブ化と非アクティブ時に移動しないテストを追加した。
- `GameStateTests` にBullet命中によるEnemy HP減少、Bullet削除、非アクティブBullet削除、死亡Enemy削除、非命中時にHPが減らないこと、命中で死亡したEnemyが削除されることを確認するテストを追加した。
- `docs/05_用語集.md` の `Bullet` に非アクティブ化と削除の説明を追記した。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。結果は45件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` にSpace入力を渡し、BulletがEnemyに命中した後にBullet表示が消え、Enemy HPが `30` から `20` に減ることを確認した。
- 連続Space入力でEnemy HPが `30`、`20`、`10` と減り、HPが0になった後にEnemy表示が消えることを確認した。

### Phase1 Step8 Score管理

- `GameState` に `Score` を追加し、初期値を0にした。
- Enemy撃破時のScore加算を `GameState.Update` 内のBullet/Enemy衝突判定処理に統合した。
- Enemyを撃破した場合のみ100点を加算し、ダメージを与えただけではScoreが増えないようにした。
- 同じEnemy撃破でScoreが二重加算されないよう、命中前後の死亡状態を確認してから加算するようにした。
- `ConsoleRenderer` で `Score: N` を表示するようにした。
- `GameStateTests` に初期Score、撃破時加算、非撃破時非加算、Enemy削除後のScore維持、二重加算なしのテストを追加した。
- `docs/05_用語集.md` に `Score` を追記した。
- Core層では Console、Thread.Sleep、ファイル直接操作、MonoGame依存を追加していない。
- ECSは導入していない。
- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。結果は50件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` にSpace入力を渡し、初期表示と撃破前は `Score: 0`、Enemy撃破後に `Score: 100` と表示されることを確認した。

### Phase1 Step9 Phase1完了確認

- `dotnet build ProjectEclipsion.sln` が成功した。
- `dotnet test ProjectEclipsion.sln` が成功した。結果は50件成功。
- `dotnet run --project src\ProjectEclipsion.App\ProjectEclipsion.App.csproj --no-build` でPhase1主要動作を確認した。
- 初期表示で `Project Eclipsion`、Player座標、HP、Shield、Score、Enemy座標、Enemy HPが表示されることを確認した。
- WASD入力でPlayer座標が変化することを確認した。
- T入力でPlayerのShieldが `50` から `40` に減り、Shield優先ダメージが動作することを確認した。
- EnemyがPlayer座標へ接近し、`EnemyAiState` が `Combat` になることを確認した。
- Space入力でBulletが発射され、Bullet座標が表示されることを確認した。
- BulletがEnemyに命中するとEnemy HPが `30`、`20`、`10` と減り、撃破後にEnemy表示が消えることを確認した。
- Enemy撃破後に `Score: 100` と表示されることを確認した。
- Core層に `Console`、`Thread.Sleep`、`System.IO` が含まれていないことを確認した。
- ECS、MonoGame、外部ゲームエンジン、新機能、巨大リファクタリングは追加していない。
- `docs/03_タスク.md` の Step9 を完了状態に更新した。
